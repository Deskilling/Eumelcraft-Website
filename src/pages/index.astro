---
import PageLayout from "@layouts/PageLayout.astro"
import TwinklingStars from "@components/TwinklingStars.astro"
import MeteorShower from "@components/MeteorShower.astro"

const features = [
  {
    title: "Test",
    description: "Mfg",
    icon: "rocket"
  }
]
---

<PageLayout title="Home" description={"Eumelcraft - advanced minecraft experience"}>

  <!-- Stars Background -->
  <div class='absolute inset-0 bg-black'>
    <div id='stars1' class='fixed inset-0'></div>
    <div id='stars2' class='fixed inset-0'></div>
    <div id='stars3' class='fixed inset-0'></div>
  </div>

  <!-- Twinkling Stars / Meteors -->
  <div id="galaxy" class="fixed inset-0">
    <div>
      <TwinklingStars/>
      <MeteorShower/>
    </div>
  </div>

  <script is:inline src="/js/bg.js"></script>

  <!-- HERO -->
  <section class="relative h-screen w-full">
    <div class="animate absolute h-full w-full flex items-center justify-center">
      <div class='relative w-full h-full flex items-center justify-center'>
        <div class='p-5 text-center'>
          <p class='animated text-2xl md:text-3xl lg:text-4xl font-bold uppercase text-white'>
            Eumelcraft
          </p>
          <p class='animated text-lg md:text-xl lg:text-2xl opacity-75'>
            advanced minecraft experience
          </p>
  <!--
          <div id="ctaButtons" class='animated flex flex-wrap gap-4 justify-center mt-5'>
            <a href='/#about' class='py-2 px-4 truncate rounded text-xs md:text-sm lg:text-base border border-white/25 bg-black/20 backdrop-blur-sm hover:bg-white/10 blend'>
              About me
            </a>
            -->
          </div>
        </div>
      </div>
    </div>
  </section>

  <!--
  <div class="relative bg-black">
    <div class="mx-auto max-w-screen-sm p-5 space-y-24 pb-16">
      <section id="about" class="animate">
        <article>
          <p> Moin Moin</p>
        </article>
      </section>
  -->
      <!-- Features Section
      <section id="features" class="animate">
        <div class="space-y-4">
          <p class="font-semibold text-white px-3 py-1 rounded border border-white/25 bg-black/20 backdrop-blur-sm inline-block">
            Features
          </p>
          <p class="mt-2">
            Discover what makes Eumelcraft special
          </p>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-5">
            {features.map(feature => (
              <div class="p-6 rounded border border-white/25 bg-black/20 backdrop-blur-sm hover:bg-white/10 transition-colors duration-300 ease-in-out">
                <div class="flex items-center mb-4">
                  <div class="p-3 rounded border border-white/25 bg-black/20 backdrop-blur-sm">
                    <svg class="size-6 fill-current">
                      <use href={`/ui.svg#${feature.icon}`}></use>
                    </svg>
                  </div>
                  <h2 class="text-xl font-semibold text-white ml-3">{feature.title}</h2>
                </div>
                <p class="text-white/75">{feature.description}</p>
              </div>
            ))}
          </div>
        </div>
      </section>
      -->


    </div>
  </div>
</PageLayout>
